#!/bin/bash
# print only errors from a remote logs which are stored in S3 or some other storage places.
curl logfile url | grep error


# for debugging the shell script
set -x


# what is crontab in linux
A file containing the schedule of various cron entries that should be run at specified times

# how to open a file in read only mode
vim -r file1.sh

